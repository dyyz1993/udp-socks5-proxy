run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - ineffassign

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - staticcheck
        - ineffassign
    - linters:
        - staticcheck
      text: "SA9003"
